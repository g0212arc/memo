# 生成物と、外に出したくないもの
# 生成物（本文そのもの）は入れない。退避用の outputs_stage0/ 等も含めて全部。
outputs*/
results/
models.txt
models_local.txt
# 作例は記事と一緒に公開する前提なので追跡する。
# 自分の作例に差し替えたい場合は下の行のコメントを外す。
# wordlists/style_examples.txt

# APIキーは絶対にコミットしない
*.key
.env

# Python
__pycache__/
*.pyc
